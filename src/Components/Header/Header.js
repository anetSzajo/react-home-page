import React from 'react';

import './header.css';

function Header(props){
    return(
        <div className="header">
            <span>Homepage</span>
            <span>App</span>
            <span>About us</span>
        </div>
    )
}

export default Header;